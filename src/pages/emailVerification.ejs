<!DOCTYPE html>
<html>
<head>
    <title>Email Verification</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
            flex-direction: column; /* add this line to align items vertically */
        }
        .container {
            text-align: center;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
            margin-bottom: 20px; /* add this line to add space between the container and the button */
        }
        .success {
            background-color: rgb(94, 227, 94);
        }
        .error {
            background-color: rgb(241, 77, 77);
        }
        h1 {
            color: #333;
        }
        p {
            color: #333;
        }
        .button {
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
            background-color: #4298f4;
            border: none;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 10%; /* make the button round */
            width: 100px; /* specify a width */
            height: 50px; /* specify a height */
            font-size: 14px; /* adjust the font size as needed */
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container <%= isSuccess ? 'success' : 'error' %>">
        <h1>Email Verification</h1>
        <p><%= message %></p>
    </div>
    
    <% if (true) { %>
        <a href="<%= loginRoute %>" class="button">Go to Login</a>
    <% } %>
</body>
</html>